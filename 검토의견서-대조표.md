# 두 검토 의견서 대조표

| 문서 | 작성 주체 | 작성 시점 |
|---|---|---|
| [검토의견서-by-Artifact.md](검토의견서-by-Artifact.md) | 클로드 아티팩트 | 2026-06-17 검수, 이후 문서화 |
| [검토의견서-by-Claude_Code.md](검토의견서-by-Claude_Code.md) | 클로드 코드 | 2026-09-03 |

두 문서는 서로 어긋나지 않음. 같은 산출물을 다른 시점에 다른 방법으로 본 것임.

---

## 1. 무엇이 달랐는가

| 항목 | 아티팩트 | 클로드 코드 |
|---|---|---|
| 검수 대상 | 조치 이전 상태와 조치 이후 상태 | 현재 게시본 상태만 |
| 검사 방법 | 자기가 쓴 코드를 읽고 대조 | 정적 분석 + Lighthouse 실측 + HTTP 응답 확인 |
| 대조 범위 | PRD 2장, 3장, 5장 | PRD 1.2절, 3장, 4.4절 |
| 항목 수 | 49개 | 10개 (치명 2, 중대 5, 경미 3) |
| 판정 기준 | PASS / WARN / FAIL | 치명 / 중대 / 경미 |
| 실행 검증 | 못 함 | 함 |

가장 큰 차이는 실행 검증임. 아티팩트는 자기가 만든 페이지를 열 수 없어 코드를 읽어 판정했음. 클로드 코드는 게시본에 실제로 요청을 보내고 Lighthouse를 돌렸음.

---

## 2. 같은 것을 본 자리

세 항목은 두 문서가 함께 짚었음.

| 항목 | 아티팩트 | 클로드 코드 |
|---|---|---|
| 이력서 PDF 부재 | 잔여 WARN 4건 중 하나. "실제 이력서 PDF 파일을 저장소에 업로드" | 치명 C-1. 게시본에서 HTTP 404 확인 |
| 이미지 자리표시자 | 잔여 WARN. 프로필 사진, 썸네일 3종 | 중대 M-4. `class="ph"` 5곳, `alt` 0건 |
| 이메일, Calendly 상수 | 잔여 WARN. `yourname@email.com`, `calendly.com/yourname` | 경미 N-2에서 Calendly 언급 |

심각도 판단이 갈림. 아티팩트는 코드가 옳게 쓰였으므로 WARN으로 두었고, 클로드 코드는 실제로 눌렀을 때 오류가 나므로 치명으로 보았음.

두 판단 모두 근거가 있음. 코드 관점에서 `<a href="resume.pdf" download="마케터_이력서.pdf">`는 올바른 구현임. 사용자 관점에서는 404임.

---

## 3. 한쪽만 본 자리

### 아티팩트만 본 것

조치 이전의 결함 14건임. 지금 파일에는 남아 있지 않아 클로드 코드가 볼 수 없었음.

| 결함 | 조치 여부 확인 |
|---|---|
| 이력서가 `<button>` 태그였음 | `<a href download>` 3곳 확인 |
| 모달 열기, 닫기 이벤트 없었음 | `data-idx`, `renderMTab`, `Escape` 확인 |
| 필터 탭 클릭 없었음 | `applyFilter`, `data-cat` 확인 |
| 카운트업 애니메이션 없었음 | `IntersectionObserver` 2곳 확인 |
| 햄버거 메뉴 없었음 | `hamburger` 7곳 확인 |
| 타이핑 애니메이션 없었음 | `typing` 4곳 확인 |
| 소셜 링크에 속성 없었음 | `target="_blank" rel="noopener"` 3곳 확인 |

조치 내역이 실제 파일에 모두 반영되어 있음을 확인했음. 아티팩트의 보고는 정확했음.

### 클로드 코드만 본 것

측정과 외부 요청이 필요한 항목임.

| 발견 | 근거 |
|---|---|
| Lighthouse Performance 80점 (PRD 목표 95점 미달) | 실측 |
| Lighthouse Accessibility 92점 (PRD 목표 90점 달성) | 실측 |
| 색 대비 미달 33건 | Lighthouse 검출 |
| `<main>` 랜드마크 없음 | 실측 |
| PRD 4.4절 SEO 항목 5개 중 4개 미반영 | `og:` 0건, `twitter:` 0건, JSON-LD 없음, robots/sitemap 없음 |
| 소셜 링크가 서비스 첫 화면으로 감 | `https://linkedin.com` 같은 도메인 주소 |
| 이력서 링크가 404 | HTTP 요청 |

---

## 4. 왜 갈렸는가

세 가지 이유임.

**첫째, 실행할 수 있는가.** 아티팩트는 자기 결과물을 열지 못함. PRD 1.2절이 정한 Lighthouse 점수는 측정 도구를 돌려야 나오므로 아티팩트가 확인할 방법이 없었음. 그래서 "Phase 1, 2 기능 전부 구현"이라고 결론지었으나, 수치 목표 달성 여부는 답하지 못했음.

**둘째, 무엇을 대조했는가.** 아티팩트는 PRD 2장, 3장, 5장의 화면 요소를 훑었음. 4.4절 SEO는 대조 범위에 넣지 않았음. 그 결과 Open Graph 부재가 49개 항목 어디에도 나오지 않음.

**셋째, 언제 봤는가.** 아티팩트는 조치 전후를 모두 봤고 클로드 코드는 지금만 봤음. 그래서 "무엇이 고쳐졌는가"는 아티팩트만, "지금 무엇이 남았는가"는 클로드 코드만 말할 수 있음.

---

## 5. 강의에 쓸 수 있는 점

이 대조 자체가 노트 2.4절이 다룬 아티팩트와 클로드 코드의 차이를 보여 주는 사례임.

| | 아티팩트 | 클로드 코드 |
|---|---|---|
| 잘하는 것 | 자기 작업 맥락을 알고 무엇을 왜 고쳤는지 설명함 | 실제로 돌려 보고 수치로 답함 |
| 못하는 것 | 만든 것을 열어 확인하지 못함 | 만들어진 경위를 모름 |

둘 중 하나가 옳은 것이 아니라 서로 보완함. 아티팩트의 49개 항목 검수가 없었다면 오늘의 산출물이 이만큼 되지 않았을 것이고, 클로드 코드의 실측이 없었다면 PRD가 정한 성능 목표에 15점 못 미친다는 사실이 드러나지 않았을 것임.
