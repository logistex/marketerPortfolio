# 검토 의견서 — 마케터 포트폴리오 웹사이트

| 항목 | 내용 |
|---|---|
| 검토 대상 | `산출물/index.html` (669줄, 35,547바이트), 게시본 <https://logistex.github.io/marketerPortfolio/> |
| 대조 기준 | `마케터 포트폴리오 웹사이트 PRD/portfolio-prd.md` |
| 검토 일자 | 2026-09-03 |
| 검토 방식 | 정적 코드 분석과 Lighthouse 실측 |

이 문서는 산출물이 만들어진 2026년 6월 당시의 검토 기록이 아님. 저장소에 검토 문서가 없어 2026년 9월에 새로 작성한 것임. 6월 검수는 대화 안에서만 이루어졌고 그 내용은 [공개 대화](https://claude.ai/share/4a18d173-c193-43fb-af0b-68eeaf61c968)에 남아 있음.

---

## 1. 요약

PRD가 수치로 정한 목표 두 가지 중 하나를 달성하고 하나를 놓쳤음. 기능 구현은 대체로 충실하나, PRD 4.4절의 SEO 항목이 거의 반영되지 않았고 콘텐츠가 자리표시자 상태로 남아 있음.

| 구분 | 건수 |
|---|---|
| 치명 (배포 상태에서 동작하지 않음) | 2 |
| 중대 (PRD 요구가 미구현) | 5 |
| 경미 (개선 여지) | 3 |

---

## 2. PRD 목표 대비 실측

PRD 1.2절이 정한 다섯 목표 중 자동으로 측정 가능한 두 가지를 Lighthouse 13.4.1로 실측했음. 나머지 셋(히어로 체류 시간, 카드 클릭률, CTA 클릭률)은 방문자 데이터가 필요해 측정하지 못했음.

| 목표 | 목표값 | 실측 | 판정 |
|---|---|---|---|
| Lighthouse Accessibility | 90 이상 | 92 | 달성 |
| Lighthouse Performance | 95 이상 | 80 | **미달** |

참고로 함께 잰 값임.

| 항목 | 점수 |
|---|---|
| Best Practices | 96 |
| SEO | 90 |

---

## 3. 치명

### C-1. 이력서 내려받기가 동작하지 않음

`href="resume.pdf"`로 링크되어 있으나 저장소에 그 파일이 없음. 게시본에서 확인한 결과 HTTP 404임.

```
https://logistex.github.io/marketerPortfolio/resume.pdf  →  404
```

PRD 3.5절이 "이력서 PDF 다운로드 버튼"을 Contact 섹션의 구성 요소로 명시했음. 방문자가 누르면 오류 화면을 보게 됨.

조치는 둘 중 하나임. 실제 PDF를 저장소에 넣거나, 파일이 준비될 때까지 버튼을 감추는 것임.

### C-2. 소셜 링크가 서비스 첫 화면으로 감

세 링크가 모두 계정 주소가 아니라 서비스 도메인임.

```html
href="https://linkedin.com"
href="https://instagram.com"
href="https://blog.naver.com"
```

누르면 본인 프로필이 아니라 각 서비스의 로그인 화면이 뜸. PRD 3.5절이 "LinkedIn / GitHub / Behance 소셜 링크"를 요구했으나, 실질적으로 연결이 끊긴 상태임.

---

## 4. 중대

### M-1. PRD 4.4절 SEO 항목이 반영되지 않음

PRD가 명시한 다섯 항목 중 넷이 없음.

| PRD 4.4절 요구 | 산출물 |
|---|---|
| Open Graph 메타 태그 | 없음 (`og:` 0건) |
| Twitter Card 메타 태그 | 없음 (`twitter:` 0건) |
| JSON-LD 구조화 데이터 | 없음 |
| `robots.txt`, `sitemap.xml` | 없음 |
| 시맨틱 HTML | 부분 반영 (`<section>` 7개, `<nav>` 2개) |

Open Graph가 없으면 카카오톡이나 슬랙에 주소를 붙여도 미리보기 카드가 뜨지 않음. PRD 1.3절이 "모바일로 먼저 확인"하는 채용 담당자를 1차 사용자로 잡았으므로, 공유 경로가 사실상 첫 관문임.

Lighthouse SEO에서도 메타 설명 누락이 지적되었음.

### M-2. 색 대비가 기준에 미달함 (33건)

Lighthouse Accessibility에서 33건이 검출되었음. 다크 배경에 회색 계열 글자(`--muted: #6B7280`)를 쓴 곳이 대부분으로 보임. 저시력 사용자가 읽기 어려움.

### M-3. `<main>` 랜드마크가 없음

스크린 리더 사용자가 반복되는 내비게이션을 건너뛰고 본문으로 갈 수 없음. `<main>` 0개, `<header>` 0개임.

### M-4. 이미지가 전부 자리표시자임

프로필 사진 1개, 소개 이미지 1개, 프로젝트 썸네일 3개가 모두 `class="ph"` 자리표시자임. `alt` 속성은 0건임.

PRD 3.1절이 "프로필 사진: 전문적이면서도 친근한 톤"을, 3.3절이 "카드 구성: 썸네일"을 요구했음. 텍스트만으로는 30초 안에 인상을 남기기 어려움.

### M-5. PRD 3.3절의 인터랙티브 차트가 CSS 막대로 대체됨

`<canvas>` 0개임. 결과 비교가 `.chart-wrap` 클래스의 CSS 막대로 구현되어 있음.

PRD 3.3절이 "Before/After 차트"를 "인터랙티브 차트"로 명시했으므로 요구 수준에 못 미침. 다만 단일 HTML 파일이라는 구현 방침에서는 합리적 절충으로 볼 수도 있음.

---

## 5. 경미

### N-1. 성능 점수가 목표에 15점 못 미침

PRD 1.2절이 95 이상을 요구했으나 80임. 단일 HTML 파일이고 이미지가 자리표시자인데도 미달인 것은 구글 폰트를 외부에서 불러오는 구조 때문으로 보임. 폰트를 자체 호스팅하거나 `font-display`를 조정하면 개선 여지가 있음.

### N-2. Calendly가 실제 주소가 아님

`calendly` 문자열이 11회 나타나나 실제 예약 계정과 연결되어 있는지 확인이 필요함. PRD 3.5절이 "30분 커피챗 예약"을 요구했음.

### N-3. 루트와 산출물 폴더에 같은 파일이 두 벌임

`index.html`과 `산출물/index.html`이 완전히 동일함(35,547바이트). 깃허브 페이지가 루트를 배포하므로 필요한 구조이나, 한쪽만 고치면 어긋남. 2026-09-02에 README에 이 사실을 명시해 두었음.

---

## 6. PRD 대비 잘 구현된 것

지적만 남기면 균형이 맞지 않으므로 함께 적음.

| PRD 요구 | 산출물 |
|---|---|
| 3.2절 카운트업 애니메이션 | `IntersectionObserver` 2회 사용으로 구현됨 |
| 3.5절 이메일 복사 버튼 | `clipboard` API로 구현됨 |
| 3.3절 카테고리 필터 | 전체 / 퍼포먼스 / 콘텐츠 / SNS 네 탭 구현됨 |
| 3.3절 프로젝트 상세 모달 | Challenge / Strategy / Execution / Result 탭 구조로 구현됨 |
| 5.4절 반응형 | 모바일 메뉴와 브레이크포인트 구현됨 |
| 2절 정보 구조 | 열 개 영역이 PRD 순서대로 배치됨 |

---

## 7. 조치 우선순위

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | C-1 이력서 404 | 방문자가 오류를 봄 |
| 2 | C-2 소셜 링크 | 연결이 끊긴 상태 |
| 3 | M-1 Open Graph | 공유 경로가 첫 관문인데 막혀 있음 |
| 4 | M-2 색 대비 | 33건이며 PRD가 접근성을 목표로 명시 |
| 5 | M-4 이미지 | 30초 인상 형성의 핵심 |

M-3, M-5, N-1은 그다음임.

---

## 8. 수업 실습본과 비교

같은 PRD에서 나온 두 산출물의 차이가 검토의 값어치를 보여 줌.

| 항목 | 완성본 | 수업 실습본 |
|---|---|---|
| Lighthouse Accessibility | 92 | 100 |
| Open Graph 태그 | 0건 | 11건 |
| Twitter Card | 0건 | 4건 |
| JSON-LD | 없음 | 있음 |
| `<main>` 랜드마크 | 없음 | 있음 |
| 이력서 링크 | 404 | 정상 |
| 프로필 이미지 | 자리표시자 | 실제 이미지 (`alt` 있음) |
| 검토 문서 | 없었음 (이 문서로 보완) | 49개 항목 검수서 |

수업 실습본은 검수 리포트를 만들고 그 결과를 반영하는 단계를 거쳤음. 그 한 단계가 위 차이를 만들었음.
