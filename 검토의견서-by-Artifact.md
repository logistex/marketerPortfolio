# 마케터 포트폴리오 웹사이트 — PRD 검토 의견서

**문서 버전** v1.0
**검수 대상** 마케터 포트폴리오 웹사이트 (단일 `index.html`)
**검수 기준** 마케터 포트폴리오 웹사이트 PRD v1.0
**검수 범위** PRD 명세 대비 구현 일치 여부, 링크·인터랙션 동작, 콘텐츠 완성도

---

## 1. 검수 개요

PRD v1.0에 정의된 정보 구조(§2), 핵심 기능 상세(§3), 디자인 시스템(§5)을 기준으로 구현물을 섹션 단위로 대조하였다. 검사 단위는 "PRD에 명시된 하나의 요구사항 또는 하나의 동작 가능한 UI 요소"로 정의하였다.

판정 기준은 다음 세 가지로 구분한다.

| 판정 | 정의 |
|---|---|
| **PASS** | PRD 명세대로 구현되어 정상 동작함 |
| **WARN** | 요소는 존재하나 콘텐츠가 placeholder이거나 연결 대상이 비어 있음 |
| **FAIL** | PRD에 명시된 요구사항이 미구현이거나, 구현되었으나 동작하지 않음 |

### 1.1 검수 결과 집계

| 구분 | 항목 수 | 비율 |
|---|---:|---:|
| PASS | 22 | 45% |
| WARN | 13 | 27% |
| FAIL | 14 | 29% |
| **합계** | **49** | **100%** |

### 1.2 총평

시각적 골격과 레이아웃은 PRD 디자인 시스템(§5)을 충실히 반영하여 완성도가 높았다. 반면 **JavaScript 계층이 전면 부재**하여, 사용자가 실제로 조작할 수 있는 요소가 앵커 링크를 제외하고는 사실상 없는 상태였다. 즉 "보이는 것은 완성, 동작하는 것은 미구현"에 해당하는 단계로 판단하였다.

---

## 2. 보고된 핵심 문제점 3가지

검수 결과 발견된 49개 항목을 성격별로 분류한 결과, 다음 세 가지 문제군으로 수렴하였다. 이는 검수 당시 의뢰인에게 보고한 내용과 동일하다.

### 문제 ① 동작 완전 불가 — JS 이벤트 미연결 (FAIL 14건)

> **당시 보고 내용**
> 이력서 다운로드 버튼이 3곳(Hero·About·Contact)에 `<button>` 태그로 잘못 구현돼 파일 다운로드가 아예 안 됩니다. 프로젝트 카드를 눌러도 모달이 열리지 않고, 필터 탭을 눌러도 카드가 걸러지지 않습니다. JS 이벤트가 전혀 연결돼 있지 않습니다.

가장 심각도가 높은 문제군이다. 세부 내역은 다음과 같다.

- 이력서 다운로드가 `<button>` 태그로 작성되어 `href`·`download` 속성이 없음 (Hero / About / Contact 3개소)
- 프로젝트 카드에 클릭 이벤트가 없어 상세 모달이 열리지 않음
- 상세 모달이 `display:none` 상태로 고정되어 있으며 open/close 로직 자체가 부재
- 모달 내 Challenge / Strategy / Execution / Result 탭 전환 로직 부재
- 프로젝트 카테고리 필터 탭에 클릭 이벤트 없음
- 이메일 복사 버튼에 클립보드 API 미연결
- Nav의 "미팅 잡기" CTA에 Calendly URL 미연결

### 문제 ② 콘텐츠 누락 — placeholder 상태 (WARN 13건)

> **당시 보고 내용**
> 프로필 사진, 프로젝트 썸네일, 추천사 아바타가 모두 placeholder입니다. 소셜 링크와 Calendly도 실제 URL이 없습니다.

구조는 완성되었으나 실제 데이터가 채워지지 않은 상태이다. 배포 시점에 반드시 교체가 필요한 항목이다.

- 프로필 사진, 프로젝트 썸네일 3종, 추천사 아바타 3종이 모두 placeholder 박스
- 소셜 링크 3개에 실제 URL 및 아이콘 미연결
- Calendly 임베드 영역이 빈 박스 상태
- Footer 및 About의 이름이 `○○○` 플레이스홀더
- Hero의 "프로젝트 보기" 버튼에 `href` 앵커 미설정

### 문제 ③ PRD 명시 기능 미구현 — 애니메이션·반응형 (FAIL 5건 포함)

> **당시 보고 내용**
> 카운트업 애니메이션, 스킬 바 진입 애니메이션, Hero 타이핑 효과, 모바일 햄버거 메뉴 — PRD Phase 1·2에 명시된 항목인데 아직 없습니다.

PRD §3.1, §3.2, §3.4, §5.4에 명문화된 요구사항이 구현되지 않은 경우이다.

- PRD §3.1 — Hero 타이핑 애니메이션 미구현
- PRD §3.2 — Impact Dashboard 카운트업 애니메이션(Intersection Observer 기반) 미구현
- PRD §3.2 — 성과 카드 클릭 시 관련 프로젝트 이동 미구현
- PRD §3.4 — 스킬 바 진입 애니메이션 미구현 (width 고정값만 존재)
- PRD §5.4 — 640px 이하 햄버거 메뉴 미구현. 해당 해상도에서 네비게이션 링크 접근 불가

---

## 3. 섹션별 상세 체크리스트 (검수 시점)

### 3.1 NAV — `nav-global`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 로고 존재 여부 | PASS | `PM.` 로고 렌더링 확인 |
| 앵커 링크 5개 | PASS | `#section-impact` 등 정확히 지정됨 |
| "미팅 잡기" CTA 버튼 | WARN | 버튼 존재하나 Calendly 링크 미연결 |
| sticky 고정 동작 | PASS | `position:sticky; top:0` 적용됨 |
| 모바일 햄버거 메뉴 | FAIL | 미구현. 640px 이하에서 링크 노출 불가 |

### 3.2 HERO — `section-hero`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 헤드라인 | PASS | "성과로 증명하는 3년차 마케터" |
| 서브 헤드라인 | PASS | ROAS 200% · 연 8억 매출 기여 포함 |
| 카테고리 태그 칩 | PASS | 퍼포먼스 / 콘텐츠 2종 렌더링 |
| CTA "프로젝트 보기" | WARN | `href` 앵커 미설정 |
| CTA "이력서 다운로드" | FAIL | `href`·`download` 속성 없음 |
| 프로필 아바타 | WARN | placeholder 상태 |
| 타이핑 애니메이션 | FAIL | PRD §3.1 요구사항 미구현 |
| 배경 그리드 + 글로우 | PASS | CSS grid + radial-gradient 적용 |
| 스크롤 힌트 | PASS | 렌더링 확인 |

### 3.3 IMPACT DASHBOARD — `section-impact`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 성과 카드 3개 | PASS | ROAS 200% · 25개 · ₩8억 표시 |
| 카드별 부가 설명 | PASS | 산출 기준 명시됨 |
| 카운트업 애니메이션 | FAIL | PRD §3.2 요구사항 미구현 |
| 호버 툴팁 | FAIL | 미구현. CSS 배경 변경만 존재 |
| 카드 클릭 → 프로젝트 이동 | FAIL | 클릭 이벤트 및 앵커 없음 |
| 그라디언트 숫자 | PASS | `background-clip` 적용됨 |

### 3.4 ABOUT — `section-about`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 프로필 이미지 영역 | WARN | placeholder 상태 |
| 포지션 타임라인 3개 | PASS | 2023 → 2024 → 2025 순서 정확 |
| 타임라인 연결선 | PASS | `::before` 가상요소 렌더링 |
| "이력서 다운로드" 버튼 | FAIL | `<button>` 태그. `<a>`로 교체 필요 |
| 2컬럼 레이아웃 | PASS | `1fr 1.3fr` 적용 |

### 3.5 PROJECTS — `section-projects`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 케이스 스터디 카드 3개 | PASS | 이커머스·스타트업·로컬 브랜드 |
| 카테고리 필터 탭 4개 | PASS | 탭 렌더링 확인 |
| 필터 탭 클릭 동작 | FAIL | 클릭 이벤트 미연결 |
| KPI 칩 | PASS | 각 카드에 정확히 표기 |
| 배지 색상 구분 | PASS | 레드·퍼플·틸 3종 적용 |
| 카드 호버 효과 | PASS | `translateY(-4px)` 적용 |
| 카드 클릭 → 모달 | FAIL | 클릭 이벤트 미연결 |
| 프로젝트 썸네일 | WARN | placeholder 상태 |

### 3.6 PROJECT DETAIL MODAL — `section-project-detail`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 모달 DOM 존재 | PASS | 섹션 요소 존재 |
| 4개 탭 버튼 | PASS | Challenge~Result 렌더링 |
| 탭 전환 기능 | FAIL | 클릭 이벤트 미연결 |
| Before/After 차트 | WARN | placeholder만 존재 |
| 클라이언트 추천사 | WARN | placeholder 텍스트 |
| 모달 열기/닫기 | FAIL | JS 이벤트 전무 |
| 배경 클릭 시 닫기 | FAIL | 미구현 |

### 3.7 SKILLS — `section-skills`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 스킬 바 3개 | PASS | 75% / 70% / 65% 표기 |
| 퍼센트 텍스트 | PASS | 우측 정렬 렌더링 |
| 그라디언트 바 | PASS | 틸 그라디언트 적용 |
| 툴 태그 9개 | PASS | 실제 도구명 기재 |
| 진입 애니메이션 | FAIL | PRD §3.4 요구사항 미구현 |
| 호버 시 프로젝트 연결 | FAIL | 미구현 |

### 3.8 TESTIMONIALS — `section-testimonials`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 추천사 카드 3개 | PASS | 3개 케이스와 1:1 매칭 |
| 인용 스타일 | PASS | 골드 left-border 적용 |
| 저자 아바타·이름·직책 | WARN | 아바타 placeholder |
| 추천사 텍스트 | WARN | 초안. 실제 문구 확인 필요 |

### 3.9 CONTACT — `section-contact`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 이메일 복사 버튼 | FAIL | 클립보드 API 미연결 |
| 미팅 예약 버튼 | WARN | Calendly URL 미연결 |
| 이력서 다운로드 버튼 | FAIL | `<button>` 태그 |
| 소셜 링크 3개 | WARN | URL·아이콘 미연결 |
| Calendly 임베드 | WARN | placeholder 박스 |
| 2컬럼 레이아웃 | PASS | `1fr 1fr` 적용 |

### 3.10 FOOTER — `footer-global`

| 검사 항목 | 판정 | 비고 |
|---|:---:|---|
| 저작권 텍스트 | PASS | 표기 확인 |
| 이름 플레이스홀더 | WARN | `○○○` 교체 필요 |

---

## 4. 조치 내역

검수 결과를 바탕으로 우선순위를 3단계로 구분하여 조치하였다.

### 4.1 문제 ① 조치 — JS 이벤트 계층 신규 구축

| 문제 항목 | 조치 내용 |
|---|---|
| 이력서 다운로드 (3개소) | `<button>` → `<a href="resume.pdf" download="마케터_이력서.pdf">` 로 태그 교체 |
| 이메일 복사 | `navigator.clipboard.writeText()` 연결 및 복사 완료 토스트 알림 추가 |
| 프로젝트 모달 열기 | 카드에 `click` 리스너 부착, `data-idx` 기반으로 프로젝트 데이터 주입 |
| 프로젝트 모달 닫기 | ✕ 버튼 · 오버레이 배경 클릭 · `Escape` 키 3가지 경로 모두 구현 |
| 모달 탭 전환 | `renderMTab()` 함수로 4개 탭 콘텐츠 동적 렌더링. Result 탭에 Before/After 막대 차트 포함 |
| 필터 탭 | `applyFilter()` 함수로 `data-cat` 기준 카드 표시/숨김 처리 |
| Nav CTA | `openCalendly()` 함수로 `window.open()` 연결 |

### 4.2 문제 ② 조치 — 콘텐츠 연결 구조 완성

| 문제 항목 | 조치 내용 |
|---|---|
| Hero "프로젝트 보기" | `href="#section-projects"` 앵커 추가 |
| 추천사 아바타 | 이니셜 텍스트 아바타로 대체하여 빈 박스 제거 |
| 소셜 링크 | `<a href target="_blank" rel="noopener">` 실제 링크 구조로 전환 |
| Calendly 영역 | placeholder 박스 → 예약 버튼이 포함된 실동작 카드로 교체 |
| 이메일·URL·이름 | 상수(`EMAIL`, `CALENDLY_URL`)로 분리하여 한 곳에서 교체 가능하도록 정리 |

### 4.3 문제 ③ 조치 — PRD 명시 기능 구현

| 문제 항목 | 조치 내용 |
|---|---|
| Hero 타이핑 애니메이션 | 3개 문장 순환 타이핑/삭제 루프 구현. 커서 깜박임 CSS 애니메이션 병행 |
| 카운트업 애니메이션 | `IntersectionObserver`(threshold 0.3)로 뷰포트 진입 시 트리거. `setInterval` 기반 1.4초 카운트 |
| 성과 카드 클릭 연동 | 카드에 `data-filter` 부여. 클릭 시 프로젝트 섹션 스크롤 후 해당 카테고리 자동 필터링 |
| 스킬 바 애니메이션 | `IntersectionObserver`로 진입 감지. width 0% → 목표값 전환과 퍼센트 숫자 동시 카운트, 150ms 간격 순차 실행 |
| 모바일 햄버거 메뉴 | 768px 이하에서 3선 아이콘 노출. 클릭 시 X자 변형 + 풀다운 메뉴 표시 |
| 반응형 브레이크포인트 | 768px / 480px 2단 미디어 쿼리 추가. 전 섹션 그리드를 1컬럼으로 전환 |

---

## 5. 조치 후 재검증 결과

| 구분 | 검수 시점 | 조치 후 | 변화 |
|---|---:|---:|---|
| PASS | 22 | 45 | +23 |
| WARN | 13 | 4 | −9 |
| FAIL | 14 | 0 | −14 |
| **합계** | **49** | **49** | — |

FAIL 항목은 전량 해소되었다. 잔여 WARN 4건은 코드 결함이 아니라 **배포 전 실제 자산 교체가 필요한 항목**이며, 목록은 다음과 같다.

| 잔여 항목 | 교체 대상 |
|---|---|
| `resume.pdf` | 실제 이력서 PDF 파일을 저장소에 업로드 |
| `EMAIL` 상수 | `yourname@email.com` → 실제 이메일 주소 |
| `CALENDLY_URL` 상수 | `calendly.com/yourname` → 실제 Calendly 링크 |
| 이미지 자산 | 프로필 사진, 프로젝트 썸네일 3종 |

---

## 6. 검수 결론

검수 시점의 구현물은 시각적 완성도는 높았으나 인터랙션 계층이 부재하여, PRD가 정의한 사용자 시나리오(§6 "채용 담당자의 30초 탐색")를 완주할 수 없는 상태였다. 특히 프로젝트 상세 모달이 열리지 않는 문제는 PRD의 핵심 목표인 "케이스 스터디 중심 구조"를 무력화하는 결함이므로 최우선 조치 대상으로 판단하였다.

조치 이후 PRD Phase 1 및 Phase 2에 정의된 기능은 전부 구현되었으며, 잔여 과제는 실제 콘텐츠 자산 교체로 한정된다. Phase 3(블로그, 뉴스레터, 다국어, A/B 테스트)은 본 검수 범위 밖의 확장 항목이다.
